# Keep the real file as .env.sports (ignored by Git and Docker builds).
# Create an app at https://www.strava.com/settings/api and request activity:read.
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REFRESH_TOKEN=
