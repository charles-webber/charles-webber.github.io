services:
  blog:
    build:
      context: .
      dockerfile: Dockerfile
    image: mozhu-blog:latest
    container_name: mozhu-blog
    restart: unless-stopped
    ports:
      - "${BLOG_PORT:-8080}:80"
